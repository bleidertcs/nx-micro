{
  "name": "prisma-netflix",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/prisma-netflix/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project prisma-netflix --web",
  "targets": {
    "prisma-generate": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx prisma generate --schema=libs/prisma-netflix/prisma/schema.prisma"
        ]
      }
    }
  }
}