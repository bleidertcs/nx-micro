{
  "name": "csv-processor-e2e",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["csv-processor"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/csv-processor-e2e/jest.config.ts",
        "passWithNoTests": true
      },
      "dependsOn": ["csv-processor:build", "csv-processor:serve", "api-gateway:serve"]
    }
  }
}
